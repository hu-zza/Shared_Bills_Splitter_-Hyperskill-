module Shared.Bills.Splitter.Hyperskill.main {
  requires clim;
  requires java.persistence;
  requires spring.boot;
  requires spring.boot.autoconfigure;
  requires spring.beans;
  requires spring.context;
  requires spring.data.commons;
}